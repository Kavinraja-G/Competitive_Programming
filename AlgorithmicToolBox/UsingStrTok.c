#include<stdio.h>
#include<string.h>

int main()
{
    // char s[] = "Hai Kavin How are you?";
    // char *p = NULL;
    // p = strtok(s," ");
    // printf("%s",p);
    char *a[1] = {"hello"};
    printf("%s", a[0]);
    return 0;
}